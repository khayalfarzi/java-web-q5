server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/sql_q5
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update